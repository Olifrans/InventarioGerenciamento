@model IEnumerable<InventarioGerenciamento.Web.Models.DAL.categorias>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.cat_Nome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.cat_Status)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.cat_Nome)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.cat_Status)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.cat_Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.cat_Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.cat_Id })
        </td>
    </tr>
}

</table>
